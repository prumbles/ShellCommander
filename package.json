{
  "name": "shellcommander",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js port=5000",
    "dev": "(cd client && npm install && npm start) & (npm install && npm start)",
    "prod": "(cd client && npm install && npm run build) && npm install && node index.js port=3000"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.18.3",
    "express": "^4.16.4",
    "shelljs": "^0.8.3"
  }
}
